# Vite 환경변수 (VITE_ 접두사만 클라이언트에 노출됨)
# 로컬 개발 시 비워두면 vite proxy 가 /api → localhost:8001 로 보내줌.
# Vercel 배포 시 Render backend URL 을 채울 것:
#   VITE_API_BASE_URL=https://fitdna-backend.onrender.com/api
VITE_API_BASE_URL=
